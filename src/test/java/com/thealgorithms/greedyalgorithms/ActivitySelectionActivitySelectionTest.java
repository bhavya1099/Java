
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-report-test using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=activitySelection_cce0da1981
ROOST_METHOD_SIG_HASH=activitySelection_79d2670611

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/java-report-test/1749810092/source/Java/src/test/java/com/thealgorithms/greedyalgorithms/ActivitySelectionTest.java
Tests:
    "@Test
@Test
public void testActivitySelection() {
    int[] start = { 1, 3, 0, 5, 8, 5 };
    int[] end = { 2, 4, 6, 7, 9, 9 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testSingleActivity() {
    int[] start = { 1 };
    int[] end = { 2 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testNoOverlap() {
    int[] start = { 1, 2, 3 };
    int[] end = { 2, 3, 4 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2));
    assertEquals(expected, result);
}
"Scenario 1: Testing the case of overlapping activities where only one can be selected.

Details:
  TestName: testOverlappingActivities
  Description: This test verifies the scenario where all activities have overlapping time intervals, ensuring that only one activity is selected based on earliest finish time.
Execution:
  Arrange: Provide start times and end times for activities that have overlapping intervals, such as `{1, 1, 1}` for start times and `{3, 3, 3}` for end times.
  Act: Invoke the `activitySelection` method on the start and end times.
  Assert: Use assertions to confirm that the result matches a list containing only one activity, `[0]`.
Validation:
  This scenario checks the correctness of the selection algorithm in a worst-case scenario where all activities overlap. It ensures that the method honors the earliest finish time principle.

Scenario 2: Testing with identical start and end times across all activities.

Details:
  TestName: testIdenticalActivities
  Description: This test ensures that the method can handle multiple activities with the exact same start and end times and only selects one.
Execution:
  Arrange: Specify start times as `{1, 1, 1}` and end times as `{2, 2, 2}`.
  Act: Run the `activitySelection` method on the provided start and end times.
  Assert: Validate the result contains a single activity `[0]`.
Validation:
  This scenario checks how the algorithm handles duplicate activities and ensures that no duplicate selection occurs.

Scenario 3: Testing with start times greater than end times.

Details:
  TestName: testInvalidActivityTimes
  Description: Verify that activities with start times greater than end times are ignored or handled robustly.
Execution:
  Arrange: Use arrays `{3, 5, 7}` for start times and `{2, 4, 6}` for end times where every start time exceeds the corresponding end time.
  Act: Invoke the `activitySelection` method with these inputs.
  Assert: Assert that the result is an empty list `[]`.
Validation:
  This test ensures the method can gracefully handle logically incorrect input where start times exceed end times and doesn't throw unexpected exceptions.

Scenario 4: Testing with no activities (empty input arrays).

Details:
  TestName: testEmptyInput
  Description: This test ensures that the method handles an empty input gracefully without exceptions.
Execution:
  Arrange: Provide empty arrays `{}` for both start times and end times.
  Act: Call the `activitySelection` method with the empty arrays.
  Assert: Verify the returned result is an empty list `[]`.
Validation:
  This edge case tests the method's robustness when given no activities to process and validates its null or empty handling logic.

Scenario 5: Testing with varying activity durations and strictly non-overlapping intervals.

Details:
  TestName: testNonOverlappingWithDifferentDurations
  Description: Validate that the method correctly selects all activities when none overlap regardless of individual activity durations.
Execution:
  Arrange: Provide meaningful data such as `{1, 3, 6}` for start times and `{2, 5, 8}` for end times with strictly non-overlapping intervals.
  Act: Execute the `activitySelection` method using the inputs.
  Assert: Confirm that all activities `[0, 1, 2]` are selected.
Validation:
  This scenario verifies that the algorithm is able to handle varying activity durations while processing non-overlapping intervals correctly.

Scenario 6: Testing the order of output indices when activities overlap partially.

Details:
  TestName: testOutputOrderOnPartialOverlap
  Description: This test ensures that selected activities are returned in the order they appear in the input array.
Execution:
  Arrange: Use `{1, 2, 3}` for start times and `{3, 5, 4}` for end times.
  Act: Call the `activitySelection` method.
  Assert: Verify the output contains `[0, 2]` which represents activities selected based on their indices and compatibility.
Validation:
  This test confirms that the algorithm respects the input activity order and selects compatible activities while adhering to finish time rules.

Scenario 7: Testing with all activities selected due to no overlapping and contiguous intervals.

Details:
  TestName: testFullySelectableActivities
  Description: Ensure that the method selects all activities when intervals are strictly contiguous and non-overlapping.
Execution:
  Arrange: Provide values such as `{1, 2, 3, 4}` for start times and `{2, 3, 4, 5}` for end times.
  Act: Invoke the `activitySelection` method.
  Assert: Confirm the result is `[0, 1, 2, 3]`.
Validation:
  The test ensures that all activities are selected when there is no overlap and the intervals are sequentially arranged.

Scenario 8: Testing with activities that are subsets of others (elastic intervals).

Details:
  TestName: testSubsetIntervals
  Description: Verify that a shorter subset activity is correctly chosen over longer activities with overlapping intervals.
Execution:
  Arrange: Utilize `{1, 2, 3}` for start times and `{10, 4, 5}` for end times where one activity spans multiple smaller ones.
  Act: Call the `activitySelection` method.
  Assert: Validate that selected activities maximize compatibility, e.g., `[1, 2]`.
Validation:
  This test ensures the selection algorithm prioritizes compatibility over duration when overlapping intervals exist.

*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Comparator;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;

class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("valid")
	public void testOverlappingActivities() {
		int[] startTimes = { 1, 1, 1 };
		int[] endTimes = { 3, 3, 3 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual, "Only one activity should be selected.");
	}

	@Test
	@Tag("valid")
	public void testIdenticalActivities() {
		int[] startTimes = { 1, 1, 1 };
		int[] endTimes = { 2, 2, 2 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual, "Only one activity should be selected when start and end times are identical.");
	}

	@Test
	@Tag("invalid")
	public void testInvalidActivityTimes() {
		int[] startTimes = { 3, 5, 7 };
		int[] endTimes = { 2, 4, 6 };
		ArrayList<Integer> expected = new ArrayList<>();

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual, "Activities with start times greater than end times should not be selected.");
	}

	@Test
	@Tag("boundary")
	public void testEmptyInput() {
		int[] startTimes = {};
		int[] endTimes = {};
		ArrayList<Integer> expected = new ArrayList<>();

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual, "Empty input should result in an empty output.");
	}

	@Test
	@Tag("valid")
	public void testNonOverlappingWithDifferentDurations() {
		int[] startTimes = { 1, 3, 6 };
		int[] endTimes = { 2, 5, 8 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2));

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual,
				"All activities should be selected when intervals are strictly non-overlapping.");
	}

	@Test
	@Tag("boundary")
	public void testOutputOrderOnPartialOverlap() {
		int[] startTimes = { 1, 2, 3 };
		int[] endTimes = { 3, 5, 4 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 2));

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual, "Selected activities should follow the input order when partially overlapping.");
	}

	@Test
	@Tag("valid")
	public void testFullySelectableActivities() {
		int[] startTimes = { 1, 2, 3, 4 };
		int[] endTimes = { 2, 3, 4, 5 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2, 3));

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual,
				"All activities should be selected when intervals are contiguous and non-overlapping.");
	}

	@Test
	@Tag("boundary")
	public void testSubsetIntervals() {
		int[] startTimes = { 1, 2, 3 };
		int[] endTimes = { 10, 4, 5 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(1, 2));

		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);

		assertEquals(expected, actual,
				"Shorter and compatible activities should be selected over overlapping longer ones.");
	}

}