
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-report-test using AI Type Claude AI and AI Model claude-3-7-sonnet-20250219

Test generated by RoostGPT for test java-report-test using AI Type Claude AI and AI Model claude-3-7-sonnet-20250219

ROOST_METHOD_HASH=activitySelection_cce0da1981
ROOST_METHOD_SIG_HASH=activitySelection_79d2670611

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/java-report-test/1742880169/source/Java/src/test/java/com/thealgorithms/greedyalgorithms/ActivitySelectionTest.java
Tests:
    "@Test
@Test
public void testActivitySelection() {
    int[] start = { 1, 3, 0, 5, 8, 5 };
    int[] end = { 2, 4, 6, 7, 9, 9 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testSingleActivity() {
    int[] start = { 1 };
    int[] end = { 2 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testNoOverlap() {
    int[] start = { 1, 2, 3 };
    int[] end = { 2, 3, 4 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2));
    assertEquals(expected, result);
}
"# Test Scenarios for ActivitySelection.activitySelection Method

```
Scenario 1: Activities with Overlapping Times

Details:
  TestName: activitiesWithOverlappingTimes
  Description: Tests the selection of non-overlapping activities when there are multiple activities with overlapping time periods. The method should select the maximum number of activities that can be performed without conflicts.
Execution:
  Arrange: Create arrays of start times and end times where some activities overlap.
  Act: Call activitySelection with the start and end time arrays.
  Assert: Verify that the returned list contains the indices of activities that do not overlap and form the maximum possible set.
Validation:
  This test verifies that the algorithm correctly selects activities by sorting them based on end times and then greedily selecting non-overlapping activities. The expected result should include activities that finish earliest and don't overlap with previously selected activities.
```

```
Scenario 2: Activities with Same End Times

Details:
  TestName: activitiesWithSameEndTimes
  Description: Tests the behavior when multiple activities have the same end time but different start times. The method should handle this case correctly.
Execution:
  Arrange: Create arrays where multiple activities end at the same time.
  Act: Call activitySelection with these arrays.
  Assert: Verify that the returned list contains the correct selection of activities.
Validation:
  This test ensures that the algorithm correctly handles the case where multiple activities have the same end time. The sorting should be stable, and the selection should still maximize the number of activities that can be performed.
```

```
Scenario 3: Activities with Same Start Times

Details:
  TestName: activitiesWithSameStartTimes
  Description: Tests the behavior when multiple activities have the same start time but different end times. The method should select the activity that ends earliest.
Execution:
  Arrange: Create arrays where multiple activities start at the same time.
  Act: Call activitySelection with these arrays.
  Assert: Verify that the returned list contains activities that end earliest among those with the same start time.
Validation:
  This test verifies that the algorithm correctly prioritizes activities that end earliest when multiple activities start at the same time, which is a key aspect of the greedy approach used in activity selection.
```

```
Scenario 4: Empty Arrays

Details:
  TestName: emptyArrays
  Description: Tests the behavior when empty arrays are provided as input. The method should handle this edge case gracefully.
Execution:
  Arrange: Create empty start and end time arrays.
  Act: Call activitySelection with these empty arrays.
  Assert: Verify that the method returns an empty ArrayList.
Validation:
  This test ensures that the method handles the edge case of empty input arrays without throwing exceptions and returns an appropriate empty result.
```

```
Scenario 5: All Activities Overlapping

Details:
  TestName: allActivitiesOverlapping
  Description: Tests the case where all activities overlap with each other. The method should select only one activity (the one that ends earliest).
Execution:
  Arrange: Create arrays where all activities overlap (each activity starts before the previous one ends).
  Act: Call activitySelection with these arrays.
  Assert: Verify that only one activity (the one with the earliest end time) is selected.
Validation:
  This test verifies that when all activities overlap, the algorithm correctly selects only the activity that ends earliest, which is the optimal solution in this scenario.
```

```
Scenario 6: Reverse Sorted Activities

Details:
  TestName: reverseSortedActivities
  Description: Tests the behavior when activities are provided in reverse order of their end times. The method should still correctly sort and select the optimal set.
Execution:
  Arrange: Create arrays where activities are in descending order of end times.
  Act: Call activitySelection with these arrays.
  Assert: Verify that the method correctly sorts and selects the optimal set of activities.
Validation:
  This test ensures that the sorting functionality works correctly regardless of the initial order of activities, which is important for the greedy algorithm to work properly.
```

```
Scenario 7: Activities with Zero Duration

Details:
  TestName: activitiesWithZeroDuration
  Description: Tests the behavior when some activities have the same start and end time (zero duration). The method should handle these activities correctly.
Execution:
  Arrange: Create arrays where some activities have the same start and end time.
  Act: Call activitySelection with these arrays.
  Assert: Verify that zero-duration activities are handled correctly in the selection process.
Validation:
  This test verifies that the algorithm correctly handles the edge case of zero-duration activities, which might be selected if they don't conflict with other activities.
```

```
Scenario 8: Large Number of Activities

Details:
  TestName: largeNumberOfActivities
  Description: Tests the performance and correctness of the method with a large number of activities. The method should efficiently select the optimal set.
Execution:
  Arrange: Create large arrays of start and end times.
  Act: Call activitySelection with these large arrays.
  Assert: Verify that the method returns the correct selection of activities within a reasonable time.
Validation:
  This test ensures that the algorithm scales well with a large number of activities and maintains correctness, which is important for real-world applications where the number of activities might be substantial.
```

```
Scenario 9: Negative Time Values

Details:
  TestName: negativeTimeValues
  Description: Tests the behavior when some activities have negative start or end times. The method should handle these values correctly.
Execution:
  Arrange: Create arrays with negative values for some start or end times.
  Act: Call activitySelection with these arrays.
  Assert: Verify that the method correctly processes activities with negative time values.
Validation:
  This test verifies that the algorithm works correctly with negative time values, which might be used to represent relative times or times before a reference point.
```

```
Scenario 10: Unequal Array Lengths

Details:
  TestName: unequalArrayLengths
  Description: Tests the behavior when the start times and end times arrays have different lengths. This should be handled as an error case.
Execution:
  Arrange: Create start and end time arrays of different lengths.
  Act: Call activitySelection with these arrays.
  Assert: Verify that the method throws an appropriate exception or handles this case gracefully.
Validation:
  This test ensures that the method properly validates its input and handles the error case of unequal array lengths, which is important for robust error handling.
```
*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.function.Executable;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import java.util.Comparator;

class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("valid")
	@DisplayName("Activities with Overlapping Times")
	void activitiesWithOverlappingTimes() {
		int[] startTimes = { 1, 3, 0, 5, 8, 5 };
		int[] endTimes = { 2, 4, 6, 7, 9, 9 };

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
		assertEquals(expected, result);
	}

	@Test
	@Tag("valid")
	@DisplayName("Activities with Same End Times")
	void activitiesWithSameEndTimes() {
		int[] startTimes = { 1, 3, 0, 5, 3, 6 };
		int[] endTimes = { 2, 4, 4, 7, 4, 9 };

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		// The algorithm should select activities that don't overlap
		// Since activities are sorted by end time, it should pick the first one with end
		// time 2,
		// then one of the activities with end time 4 (whichever comes first after
		// sorting),
		// then the activity with end time 7, and finally the one with end time 9
		assertTrue(result.contains(0)); // Activity with end time 2
		assertEquals(4, result.size());
	}

	@Test
	@Tag("valid")
	@DisplayName("Activities with Same Start Times")
	void activitiesWithSameStartTimes() {
		int[] startTimes = { 1, 1, 1, 5, 5, 8 };
		int[] endTimes = { 3, 4, 2, 7, 9, 10 };

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		// The algorithm should select the activity that ends earliest among those with
		// the same start time
		assertTrue(result.contains(2)); // Activity with start time 1 and end time 2
		assertTrue(result.contains(3) || result.contains(4)); // One of the activities
																// starting at 5
		assertTrue(result.contains(5)); // Activity starting at 8
	}

	@Test
	@Tag("boundary")
	@DisplayName("Empty Arrays")
	void emptyArrays() {
		int[] startTimes = {};
		int[] endTimes = {};

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		assertTrue(result.isEmpty());
	}

	@Test
	@Tag("valid")
	@DisplayName("All Activities Overlapping")
	void allActivitiesOverlapping() {
		int[] startTimes = { 1, 2, 3, 4, 5 };
		int[] endTimes = { 10, 11, 12, 13, 14 };

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		// Only one activity should be selected (the one with earliest end time)
		assertEquals(1, result.size());
		assertEquals(0, result.get(0)); // Activity with end time 10
	}

	@Test
	@Tag("valid")
	@DisplayName("Reverse Sorted Activities")
	void reverseSortedActivities() {
		int[] startTimes = { 1, 3, 5, 7, 9 };
		int[] endTimes = { 10, 8, 6, 4, 2 };

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		// The algorithm should sort by end times and select non-overlapping activities
		assertEquals(5, result.size());
		assertTrue(result.contains(4)); // Activity with end time 2
		assertTrue(result.contains(3)); // Activity with end time 4
		assertTrue(result.contains(2)); // Activity with end time 6
		assertTrue(result.contains(1)); // Activity with end time 8
		assertTrue(result.contains(0)); // Activity with end time 10
	}

	@Test
	@Tag("boundary")
	@DisplayName("Activities with Zero Duration")
	void activitiesWithZeroDuration() {
		int[] startTimes = { 1, 2, 3, 4, 5, 6 };
		int[] endTimes = { 2, 2, 4, 4, 7, 6 };

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		// Zero-duration activities (start time = end time) should be handled correctly
		assertTrue(result.contains(0) || result.contains(1)); // One of the activities
																// ending at 2
		assertTrue(result.contains(2) || result.contains(3)); // One of the activities
																// ending at 4
		assertTrue(result.contains(5)); // Activity with start and end time 6
		assertTrue(result.contains(4)); // Activity ending at 7
	}

	@Test
	@Tag("valid")
	@DisplayName("Large Number of Activities")
	void largeNumberOfActivities() {
		int[] startTimes = new int[1000];
		int[] endTimes = new int[1000];

		for (int i = 0; i < 1000; i++) {
			startTimes[i] = i * 2;
			endTimes[i] = i * 2 + 3;
		}

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		// For this pattern, we expect activities to be selected with a gap of 1
		assertFalse(result.isEmpty());
		assertTrue(result.size() > 300); // Rough estimate of expected size
	}

	@Test
	@Tag("valid")
	@DisplayName("Negative Time Values")
	void negativeTimeValues() {
		int[] startTimes = { -5, -3, -1, 1, 3 };
		int[] endTimes = { -3, -1, 1, 3, 5 };

		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

		// All activities are non-overlapping, so all should be selected
		assertEquals(5, result.size());
		assertEquals(Arrays.asList(0, 1, 2, 3, 4), result);
	}

	@Test
	@Tag("invalid")
	@DisplayName("Unequal Array Lengths")
	void unequalArrayLengths() {
		int[] startTimes = { 1, 3, 0, 5, 8 };
		int[] endTimes = { 2, 4, 6, 7 };

		Executable execution = () -> ActivitySelection.activitySelection(startTimes, endTimes);

		// Expecting an ArrayIndexOutOfBoundsException since the method tries to access
		// endTimes[4]
		assertThrows(ArrayIndexOutOfBoundsException.class, execution);
	}

}