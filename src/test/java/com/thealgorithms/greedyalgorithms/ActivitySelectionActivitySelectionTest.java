
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-unit-test using AI Type  and AI Model

ROOST_METHOD_HASH=activitySelection_77631a6e2d
ROOST_METHOD_SIG_HASH=activitySelection_bf79d28a9f

"""
Scenario 1: Test with valid start times and end times

Details:
  TestName: testActivitySelectionWithValidTimes
  Description: This test is meant to check the activity selection functionality with valid start times and end times. The target scenario is when the start times and end times are provided in a way that multiple activities can be selected.
Execution:
  Arrange: Set up start times and end times arrays with valid values.
  Act: Invoke activitySelection method with the start times and end times arrays.
  Assert: Use JUnit assertions to compare the actual selected activities against the expected activities.
Validation:
  The assertion aims to verify that the activity selection is done correctly based on the start times and end times. The expected result is the activities that have the earliest end times and do not overlap with each other. This test is significant as it verifies the core functionality of the activity selection.

Scenario 2: Test with overlapping activities

Details:
  TestName: testActivitySelectionWithOverlappingActivities
  Description: This test is meant to check the activity selection functionality with overlapping activities. The target scenario is when the start times and end times are provided in a way that some activities overlap with each other.
Execution:
  Arrange: Set up start times and end times arrays with overlapping activities.
  Act: Invoke activitySelection method with the start times and end times arrays.
  Assert: Use JUnit assertions to compare the actual selected activities against the expected activities.
Validation:
  The assertion aims to verify that the activity selection is done correctly in the presence of overlapping activities. The expected result is the activities that have the earliest end times and do not overlap with each other. This test is significant as it verifies the functionality of the activity selection in a more complex scenario.

Scenario 3: Test with empty arrays

Details:
  TestName: testActivitySelectionWithEmptyArrays
  Description: This test is meant to check the activity selection functionality when no activities are provided. The target scenario is when the start times and end times arrays are empty.
Execution:
  Arrange: Set up empty start times and end times arrays.
  Act: Invoke activitySelection method with the empty arrays.
  Assert: Use JUnit assertions to compare the actual selected activities against the expected activities.
Validation:
  The assertion aims to verify that the activity selection handles empty arrays correctly. The expected result is an empty list of selected activities. This test is significant as it verifies the edge case where no activities are provided.

Scenario 4: Test with single activity

Details:
  TestName: testActivitySelectionWithSingleActivity
  Description: This test is meant to check the activity selection functionality when only a single activity is provided. The target scenario is when the start times and end times arrays contain only one activity.
Execution:
  Arrange: Set up start times and end times arrays with a single activity.
  Act: Invoke activitySelection method with the arrays.
  Assert: Use JUnit assertions to compare the actual selected activities against the expected activities.
Validation:
  The assertion aims to verify that the activity selection handles single activity correctly. The expected result is a list with the single activity. This test is significant as it verifies the edge case where only one activity is provided.
"""
*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.util.ArrayList;
import java.util.Arrays;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.*;
import java.util.Comparator;

public class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("valid")
	public void testActivitySelectionWithValidTimes() {
		int[] startTimes = { 1, 3, 0, 5, 8, 5 };
		int[] endTimes = { 2, 4, 6, 7, 9, 9 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, actual, "The selected activities did not match the expected activities.");
	}

	@Test
	@Tag("valid")
	public void testActivitySelectionWithOverlappingActivities() {
		int[] startTimes = { 1, 3, 0, 5, 8, 5 };
		int[] endTimes = { 2, 4, 6, 7, 10, 9 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3));
		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, actual, "The selected activities did not match the expected activities.");
	}

	@Test
	@Tag("boundary")
	public void testActivitySelectionWithEmptyArrays() {
		int[] startTimes = {};
		int[] endTimes = {};
		ArrayList<Integer> expected = new ArrayList<>();
		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, actual, "The selected activities did not match the expected activities.");
	}

	@Test
	@Tag("boundary")
	public void testActivitySelectionWithSingleActivity() {
		int[] startTimes = { 1 };
		int[] endTimes = { 2 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));
		ArrayList<Integer> actual = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, actual, "The selected activities did not match the expected activities.");
	}

}