
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-unit-test using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=activitySelection_cce0da1981
ROOST_METHOD_SIG_HASH=activitySelection_79d2670611

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/java-unit-test/4e9bb5c0-a1ac-4f9a-b619-5732e81d4ede/source/Java/src/test/java/com/thealgorithms/greedyalgorithms/ActivitySelectionTest.java
Tests:
    "@Test
@Test
public void testActivitySelection() {
    int[] start = { 1, 3, 0, 5, 8, 5 };
    int[] end = { 2, 4, 6, 7, 9, 9 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testSingleActivity() {
    int[] start = { 1 };
    int[] end = { 2 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testNoOverlap() {
    int[] start = { 1, 2, 3 };
    int[] end = { 2, 3, 4 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2));
    assertEquals(expected, result);
}
"
Scenario 9: Activities Sorted by Start Time Instead of End Time

Details:
  TestName: handleActivitiesSortedByStartTime
  Description: Validates the algorithm’s handling of activities when input arrays are sorted by start times rather than end times. Ensures proper selection after sorting.

Execution:
  Arrange: Provide input arrays sorted by start times, e.g., `startTimes = {1, 2, 3, 4}` and `endTimes = {5, 3, 4, 6}`.
  Act: Invoke the `activitySelection` method with these inputs.
  Assert: Verify that the result is correct, considering non-overlapping activities after internal sorting by end time.

Validation:
  Ensures the method sorts activities correctly by end times and selects activities with adherence to the constraints. Verifies proper functioning regardless of input ordering.


*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Tag;
import java.util.ArrayList;
import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.jupiter.api.*;
import java.util.Arrays;
import java.util.Comparator;

class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("valid")
	public void handleActivitiesSortedByStartTime() {
		// Arrange
		int[] startTimes = { 1, 2, 3, 4 };
		int[] endTimes = { 5, 3, 4, 6 };

		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		ArrayList<Integer> expectedResult = new ArrayList<>();
		expectedResult.add(1); // Activity with index 1
		expectedResult.add(2); // Activity with index 2
		assertEquals(expectedResult, result);
	}

	@Test
	@Tag("boundary")
	public void handleEmptyArrays() {
		// Arrange
		int[] startTimes = {};
		int[] endTimes = {};
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		ArrayList<Integer> expectedResult = new ArrayList<>();
		assertEquals(expectedResult, result);
	}

	@Test
	@Tag("invalid")
	public void handleUnequalLengthArrays() {
		// Arrange
		int[] startTimes = { 1, 2 };
		int[] endTimes = { 3 };
		// Act & Assert
		try {
			ActivitySelection.activitySelection(startTimes, endTimes);
		}
		catch (IllegalArgumentException e) {
			assertEquals("Invalid input: Arrays must have the same length.", e.getMessage());
		}
		catch (Exception e) {
			assertEquals(null, e.getMessage()); // Ensure no arbitrary exceptions are
												// thrown
		}
	}

	@Test
	@Tag("valid")
	public void handleOverlappingActivities() {
		// Arrange
		int[] startTimes = { 1, 3, 2, 5 };
		int[] endTimes = { 2, 4, 3, 6 };
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		ArrayList<Integer> expectedResult = new ArrayList<>();
		expectedResult.add(0); // Activity with index 0
		expectedResult.add(1); // Activity with index 1
		expectedResult.add(3); // Activity with index 3
		assertEquals(expectedResult, result);
	}

	@Test
	@Tag("boundary")
	public void handleSingleActivity() {
		// Arrange
		int[] startTimes = { 1 };
		int[] endTimes = { 3 };
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		ArrayList<Integer> expectedResult = new ArrayList<>();
		expectedResult.add(0); // Only activity with index 0
		assertEquals(expectedResult, result);
	}

	@Test
	@Tag("valid")
	public void handleNoOverlappingActivities() {
		// Arrange
		int[] startTimes = { 1, 3, 5, 7 };
		int[] endTimes = { 2, 4, 6, 8 };
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		ArrayList<Integer> expectedResult = new ArrayList<>();
		expectedResult.add(0); // Activity with index 0
		expectedResult.add(1); // Activity with index 1
		expectedResult.add(2); // Activity with index 2
		expectedResult.add(3); // Activity with index 3
		assertEquals(expectedResult, result);
	}

}