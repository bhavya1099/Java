
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-report-test using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=activitySelection_c61ff7b9c7
ROOST_METHOD_SIG_HASH=activitySelection_739fe8f72f

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/java-report-test/1751875366/source/Java/src/test/java/com/thealgorithms/greedyalgorithms/ActivitySelectionActivitySelectionTest.java
Tests:
    "@Test
@Test
@Tag("valid")
public void testCompletelyOverlappingActivities() {

    int[] startTimes = { 1, 2, 3 };
    int[] endTimes = { 4, 4, 4 };

    ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

    assertNotNull(result, "The result should not be null.");
    assertEquals(1, result.size(), "Only one activity should be selected.");
    assertEquals((Integer) 0, result.get(0), "The first activity index should be selected.");
}
"
    "@Test
@Test
@Tag("boundary")
public void testSingleActivity() {

    int[] startTimes = { 1 };
    int[] endTimes = { 2 };

    ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

    assertNotNull(result, "The result should not be null.");
    assertEquals(1, result.size(), "Only one activity should be selected.");
    assertEquals((Integer) 0, result.get(0), "The single activity index should be selected.");
}
"
    "@Test
@Test
@Tag("valid")
public void testNonOverlappingActivities() {

    int[] startTimes = { 1, 3, 5 };
    int[] endTimes = { 2, 4, 6 };

    ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

    assertNotNull(result, "The result should not be null.");
    assertEquals(3, result.size(), "All activities should be selected.");
    assertEquals((Integer) 0, result.get(0), "First activity index should match.");
    assertEquals((Integer) 1, result.get(1), "Second activity index should match.");
    assertEquals((Integer) 2, result.get(2), "Third activity index should match.");
}
"
    "@Test
@Test
@Tag("invalid")
public void testEmptyActivities() {

    int[] startTimes = {};
    int[] endTimes = {};

    ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

    assertNotNull(result, "The result should not be null.");
    assertEquals(0, result.size(), "No activities should be selected.");
}
"
    "@Test
@Test
@Tag("boundary")
public void testEdgeCaseWithSameStartAndEndTimes() {

    int[] startTimes = { 1, 1, 1 };
    int[] endTimes = { 2, 2, 2 };

    ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

    assertNotNull(result, "The result should not be null.");
    assertEquals(1, result.size(), "Only one activity should be selected.");
    assertEquals((Integer) 0, result.get(0), "The first activity index should be selected.");
}
"
    "@Test
@Test
@Tag("valid")
public void testMixedOverlapsAndNonOverlaps() {

    int[] startTimes = { 1, 2, 3, 5 };
    int[] endTimes = { 2, 5, 4, 6 };

    ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

    assertNotNull(result, "The result should not be null.");
    assertEquals(2, result.size(), "Only two activities should be selected.");
    assertEquals((Integer) 0, result.get(0), "First activity index should match.");
    assertEquals((Integer) 3, result.get(1), "Fourth activity index should match.");
}
"
    "@Test
@Test
@Tag("integration")
public void testLargeNumberOfActivities() {

    int[] startTimes = { 1, 3, 0, 5, 8, 5 };
    int[] endTimes = { 2, 4, 6, 7, 9, 9 };

    ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);

    assertNotNull(result, "The result should not be null.");
    assertEquals(4, result.size(), "Four activities should be selected.");
    assertEquals((Integer) 0, result.get(0), "First activity index should match.");
    assertEquals((Integer) 1, result.get(1), "Second activity index should match.");
    assertEquals((Integer) 3, result.get(2), "Fourth activity index should match.");
    assertEquals((Integer) 4, result.get(3), "Fifth activity index should match.");
}
"

File Path: /var/tmp/Roost/RoostGPT/java-report-test/1751875366/source/Java/src/test/java/com/thealgorithms/greedyalgorithms/ActivitySelectionTest.java
Tests:
    "@Test
@Test
public void testActivitySelection() {
    int[] start = { 1, 3, 0, 5, 8, 5 };
    int[] end = { 2, 4, 6, 7, 9, 9 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testSingleActivity() {
    int[] start = { 1 };
    int[] end = { 2 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testNoOverlap() {
    int[] start = { 1, 2, 3 };
    int[] end = { 2, 3, 4 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2));
    assertEquals(expected, result);
}
"Scenario [1]: Test with activities having invalid negative start and end times

Details:
  TestName: testNegativeStartAndEndTimes
  Description: Verify the behavior of the method when provided with invalid negative start and end times. Ensure that the system can handle and return appropriate output without errors.

Execution:
  Arrange: Create input arrays with negative start and end times such as startTimes = {-1, -5, -3} and endTimes = {-2, -6, -4}.
  Act: Invoke the activitySelection method with these parameters.
  Assert: Verify that the output is an empty list since the negative values are logically invalid for activity timings.

Validation:
  The assertion ensures the activitySelection method gracefully handles invalid inputs without crashing or returning unexpected results. This test validates the method's robustness and ensures input sanitization or logical handling for edge cases.

---

Scenario [2]: Test with no overlap and identical activity durations

Details:
  TestName: testIdenticalDurationsNoOverlap
  Description: Verify the method's ability to select multiple activities where all have identical durations but no overlapping times.

Execution:
  Arrange: Create input arrays such as startTimes = {1, 4, 7} and endTimes = {3, 6, 9} where no time ranges overlap but all durations are identical.
  Act: Invoke the activitySelection method with the given arrays.
  Assert: Validate that all activity indices are selected, as none of the activities conflict.

Validation:
  Ensures the activitySelection method can handle scenarios where all available activities can be accommodated due to non-overlapping conditions.

---

Scenario [3]: Test with overlapping activities partially covering each other

Details:
  TestName: testPartialOverlapActivities
  Description: Check the behavior when some activities partially overlap and others do not.

Execution:
  Arrange: Provide input arrays such as startTimes = {1, 2, 3, 6} and endTimes = {4, 5, 6, 8} where partial overlaps occur.
  Act: Invoke the activitySelection method with these arrays.
  Assert: Confirm the correct selection of a subset of activities that maximizes the count without overlap (e.g., [0, 3]).

Validation:
  Verifies the ability of the method to handle test cases involving partial overlaps, ensuring it effectively skips conflicting activities while selecting non-conflicting ones.

---

Scenario [4]: Test with all activities ending at the same time but starting at different times

Details:
  TestName: testActivitiesEndingAtSameTime
  Description: Analyze how the method processes multiple activities having the same end time but different start times.

Execution:
  Arrange: Prepare input arrays like startTimes = {1, 2, 3, 4} and endTimes = {5, 5, 5, 5}.
  Act: Call the activitySelection method with these parameters.
  Assert: Check that only one activity is selected (e.g., [0]) as it maximizes the timeline by the earliest start.

Validation:
  Confirms the method's approach of choosing activities based on minimum overlap and earliest start time in cases of identical end times.

---

Scenario [5]: Edge case with all activities sharing the same start and end times

Details:
  TestName: testAllActivitiesSameStartEndTimes
  Description: Test the method's behavior when all activities have identical start and end times.

Execution:
  Arrange: Create inputs such as startTimes = {1, 1, 1} and endTimes = {2, 2, 2}.
  Act: Invoke the activitySelection method with these inputs.
  Assert: Verify that only one activity is selected (e.g., [0]).

Validation:
  Checks the method's ability to handle scenarios with complete overlap where no distinct activity can be selected except one.

---

Scenario [6]: Test with non-overlapping activities in random order of start and end times

Details:
  TestName: testRandomOrderNonOverlapping
  Description: Verify that the method effectively selects activities even when the input times are unsorted.

Execution:
  Arrange: Provide unsorted input arrays such as startTimes = {5, 1, 3} and endTimes = {6, 2, 4}.
  Act: Call activitySelection with these inputs.
  Assert: Ensure that all activities are selected and returned in the correct order based on their timing (e.g., [1, 2, 0]).

Validation:
  Ensures the method efficiently sorts and processes activities irrespective of input order, accurately reflecting the functionality of internal sorting logic.

---

Scenario [7]: Test with one activity spanning across all other activities' time ranges

Details:
  TestName: testActivityEnclosingOthers
  Description: Check that the method correctly selects activities when one spans the entire time duration of other activities.

Execution:
  Arrange: Provide input arrays such as startTimes = {1, 2, 3} and endTimes = {10, 4, 5}.
  Act: Call activitySelection with the given inputs.
  Assert: Validate that only the activities not directly overlapped by the spanning activity are selected (e.g., [0]).

Validation:
  Tests the logic for identifying non-conflicting activities when one activity dominates others due to its longer duration.

---

Scenario [8]: Test with a very large number of activities

Details:
  TestName: testLargeNumberActivities
  Description: Validate the performance and correctness of the method when processing a large number of activities.

Execution:
  Arrange: Create large input arrays with 1000+ startTimes and endTimes representing realistic random data.
  Act: Call activitySelection with these inputs.
  Assert: Ensure the selected activities are a non-overlapping subset and verify performance efficiency.

Validation:
  Validates scalability, performance, and correctness when handling large datasets, ensuring the method meets real-world requirements.

*/

// ********RoostGPT********
package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Comparator;

public class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("valid")
	public void testActivitiesWithSameEndTime() {
		// Arrange
		int[] startTimes = { 1, 2, 3, 4 };
		int[] endTimes = { 5, 5, 5, 5 };
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertNotNull(result, "The result should not be null.");
		assertEquals(1, result.size(), "Only one activity should be selected.");
		assertEquals((Integer) 0, result.get(0), "The first activity index should be selected.");
	}

	@Test
	@Tag("edge-case")
	public void testEmptyStartAndEndTimeArrays() {
		// Arrange
		int[] startTimes = {};
		int[] endTimes = {};
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertNotNull(result, "The result should not be null.");
		assertEquals(0, result.size(), "No activities should be selected for empty input.");
	}

	@Test
	@Tag("edge-case")
	public void testSingleActivity() {
		// Arrange
		int[] startTimes = { 1 };
		int[] endTimes = { 2 };
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertNotNull(result, "The result should not be null.");
		assertEquals(1, result.size(), "Only one activity should be selected.");
		assertEquals((Integer) 0, result.get(0), "The single activity index should be selected.");
	}

	@Test
	@Tag("valid")
	public void testActivitiesWithOverlappingTimes() {
		// Arrange
		int[] startTimes = { 1, 3, 2, 5 };
		int[] endTimes = { 2, 4, 3, 6 };
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertNotNull(result, "The result should not be null.");
		assertEquals(3, result.size(), "Three activities should be selected.");

		// Validate selected indexes
		assertTrue(result.contains((Integer) 0), "Activity 0 should be selected.");
		assertTrue(result.contains((Integer) 2), "Activity 2 should be selected.");
		assertTrue(result.contains((Integer) 3), "Activity 3 should be selected.");
	}

	@Test
	@Tag("edge-case")
	public void testActivitiesWithIdenticalStartEndTimes() {
		// Arrange
		int[] startTimes = { 1, 1, 1 };
		int[] endTimes = { 2, 2, 2 };
		// Act
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertNotNull(result, "The result should not be null.");
		assertEquals(1, result.size(), "Only one activity should be selected out of identical activities.");
		assertEquals((Integer) 0, result.get(0), "The first activity index should be selected.");
	}

}