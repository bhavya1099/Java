
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model

ROOST_METHOD_HASH=activitySelection_77631a6e2d
ROOST_METHOD_SIG_HASH=activitySelection_bf79d28a9f

```
Scenario 1: No Activities Provided

Details:
  TestName: testNoActivitiesProvided
  Description: Tests the behavior of the activitySelection method when no activities are provided (empty arrays for startTimes and endTimes).
Execution:
  Arrange: Initialize empty arrays for startTimes and endTimes.
  Act: Call activitySelection with the empty arrays.
  Assert: Verify that the result is an empty list.
Validation:
  The assertion checks that the list of selected activities is empty because there are no activities to select from. This test confirms that the method handles empty input gracefully without errors.

Scenario 2: Single Activity Provided

Details:
  TestName: testSingleActivityProvided
  Description: Tests the activitySelection method with a single activity to ensure it selects that activity.
Execution:
  Arrange: Provide arrays with one start time and one end time.
  Act: Call activitySelection with these arrays.
  Assert: Verify that the result contains the index of the single activity.
Validation:
  The assertion verifies that the list contains exactly one activity, which is the provided one. This test confirms that the method can handle and correctly process the smallest non-empty case.

Scenario 3: Multiple Non-Overlapping Activities

Details:
  TestName: testMultipleNonOverlappingActivities
  Description: Tests the activitySelection method with multiple activities that do not overlap, expecting all to be selected.
Execution:
  Arrange: Provide startTimes and endTimes such that all activities are non-overlapping.
  Act: Call activitySelection with these parameters.
  Assert: Verify that the result contains all activity indices.
Validation:
  The assertion checks that all activities are selected. This test confirms the method's ability to identify and select all non-overlapping activities.

Scenario 4: Multiple Activities with Some Overlapping

Details:
  TestName: testActivitiesWithSomeOverlapping
  Description: Tests how the activitySelection method handles a mix of overlapping and non-overlapping activities.
Execution:
  Arrange: Provide startTimes and endTimes where some activities overlap.
  Act: Call activitySelection.
  Assert: Verify that the result contains only the indices of activities that do not overlap.
Validation:
  The assertion verifies that only non-overlapping activities, based on end times, are selected. This test is significant for validating the method's core functionality of activity selection based on optimal use of time.

Scenario 5: Activities with Same End Time

Details:
  TestName: testActivitiesWithSameEndTime
  Description: Test the scenario where multiple activities have the same end time but different start times.
Execution:
  Arrange: Start and end times are set such that multiple activities end at the same time.
  Act: Call activitySelection.
  Assert: Verify the selection criteria in such a scenario.
Validation:
  The assertion checks which activities are selected when end times clash but start times differ. This test evaluates the method's decision-making in scenarios of time conflict.

Scenario 6: Activities Provided in Descending Order of End Time

Details:
  TestName: testActivitiesInDescendingOrder
  Description: Test the activitySelection method's ability to handle input where activities are initially provided in descending order of their end times.
Execution:
  Arrange: Provide startTimes and endTimes in descending order.
  Act: Call activitySelection.
  Assert: Verify that the activities are selected correctly.
Validation:
  This test checks the method's sorting mechanism and ensures that activities are considered in the correct order, confirming the method's robustness in handling unordered input.
```
*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Tag;
import static org.assertj.core.api.Assertions.assertThat;
import java.util.ArrayList;
import org.junit.jupiter.api.*;
import java.util.Arrays;
import java.util.Comparator;

public class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("valid")
	public void testNoActivitiesProvided() {
		int[] startTimes = {};
		int[] endTimes = {};
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(result).isEmpty();
	}

	@Test
	@Tag("valid")
	public void testSingleActivityProvided() {
		int[] startTimes = { 1 };
		int[] endTimes = { 2 };
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(result).containsExactly(0);
	}

	@Test
	@Tag("valid")
	public void testMultipleNonOverlappingActivities() {
		int[] startTimes = { 1, 3, 5 };
		int[] endTimes = { 2, 4, 6 };
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(result).containsExactly(0, 1, 2);
	}

	@Test
	@Tag("valid")
	public void testActivitiesWithSomeOverlapping() {
		int[] startTimes = { 1, 3, 5, 6 };
		int[] endTimes = { 4, 5, 7, 8 };
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(result).containsExactly(0, 2, 3);
	}

	@Test
	@Tag("valid")
	public void testActivitiesWithSameEndTime() {
		int[] startTimes = { 1, 2, 4 };
		int[] endTimes = { 5, 5, 5 };
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(result).containsExactly(0);
	}

	@Test
	@Tag("valid")
	public void testActivitiesInDescendingOrder() {
		int[] startTimes = { 5, 3, 1 };
		int[] endTimes = { 8, 6, 4 };
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(result).containsExactly(2, 1, 0);
	}

}