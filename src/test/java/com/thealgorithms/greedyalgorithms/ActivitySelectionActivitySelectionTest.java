
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-customannotation-test using AI Type  and AI Model

ROOST_METHOD_HASH=activitySelection_77631a6e2d
ROOST_METHOD_SIG_HASH=activitySelection_bf79d28a9f

```
Scenario 1: No Activities

Details:
  TestName: noActivitiesSelected
  Description: Test to ensure the method works correctly with an empty input array, resulting in an empty list of selected activities.
Execution:
  Arrange: Initialize startTimes and endTimes with empty arrays.
  Act: Call activitySelection method with the empty arrays.
  Assert: Assert that the returned list of selected activities is empty.
Validation:
  This confirms that the method can handle cases where no activities are provided, a common edge-case scenario ensuring stability in production with varied inputs.
  Ensuring the method can handle empty inputs without errors is critical for robustness.

Scenario 2: Activities With No Overlap

Details:
  TestName: activitiesWithNoOverlap
  Description: Test with activities that do not overlap to confirm all are selected.
Execution:
  Arrange: Provide startTimes and endTimes arrays where no times overlap.
  Act: Invoke activitySelection method.
  Assert: Assert that all activities are selected.
Validation:
  Assertive check ensures that the algorithm correctly identifies non-overlapping activities. This scenario tests the algorithm's capability to recognize and select all non-overlapping activities, simulating a best-case performance situation.

Scenario 3: Activities With Complete Overlap

Details:
  TestName: activitiesWithCompleteOverlap
  Description: Test with activities that all overlap completely, expecting only one to be chosen.
Execution:
  Arrange: Set startTimes and endTimes where all activities start and end at the same time.
  Act: Call the activitySelection method.
  Assert: Assert that only one activity is selected.
Validation:
  This test verifies the method's handling of worst-case overlap, where only one activity can logically be chosen. It checks the robustness of selection criteria under extreme conditions.

Scenario 4: Negative Time Inputs

Details:
  TestName: negativeTimeInputs
  Description: Ensure the method handles scenarios where start and/or end times are negative.
Execution:
  Arrange: Provide startTimes and endTimes containing negative values.
  Act: Invoke the activitySelection method.
  Assert: Check for expected behavior, possibly an empty list or handling of negative values.
Validation:
  Helps validate if the method can gracefully handle erroneous input such as negative time values, pertinent for ensuring data validation and error handling capabilities in the functionality.

Scenario 5: Activities That End Before They Start

Details:
  TestName: activitiesEndBeforeStart
  Description: Test activities where some end times are earlier than start times; expect these to be ignored or handled specifically.
Execution:
  Arrange: Setup startTimes and endTimes where at least one activity has an end time before its start time.
  Act: Call activitySelection.
  Assert: Confirm that the activity with inverted times is not selected.
Validation:
  This tests the method's ability to ignore or properly handle logically incorrect data where an activity ends before it starts, crucial for maintaining data integrity in selections.

Scenario 6: Concurrently Ending Activities With Different Starts

Details:
  TestName: concurrentlyEndingActivitiesWithDifferentStarts
  Description: Verify that when multiple activities end at the same time but start at different times, the selection works correctly.
Execution:
  Arrange: Provide startTimes and endTimes with activities having the same end time but different start times.
  Act: Invoke activitySelection.
  Assert: Assert that correct activities based on start times comparison are selected.
Validation:
  This checks the selection logic's capability to handle activities ending concurrently, ensuring the most algorithmically optimal selection of activities based on their start times.
```
*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Tag;
import java.util.ArrayList;
import static org.junit.jupiter.api.Assertions.assertTrue;
import static org.assertj.core.api.Assertions.assertThat;
import org.junit.jupiter.api.*;
import java.util.Arrays;
import java.util.Comparator;

public class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("boundary")
	public void noActivitiesSelected() {
		int[] startTimes = {};
		int[] endTimes = {};
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		assertTrue(selectedActivities.isEmpty(), "Expected no activities to be selected");
	}

	@Test
	@Tag("valid")
	public void activitiesWithNoOverlap() {
		int[] startTimes = { 1, 3, 5 };
		int[] endTimes = { 2, 4, 6 };
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(selectedActivities).containsExactly(0, 1, 2);
	}

	@Test
	@Tag("valid")
	public void activitiesWithCompleteOverlap() {
		int[] startTimes = { 1, 1, 1 };
		int[] endTimes = { 2, 2, 2 };
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(selectedActivities).containsExactly(0);
	}

	@Test
	@Tag("invalid")
	public void negativeTimeInputs() {
		int[] startTimes = { -1, -3, -4 };
		int[] endTimes = { 2, 5, 10 };
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(selectedActivities).isEmpty(); // TODO: Change behavior based on actual
													// method handling of negative times
	}

	@Test
	@Tag("boundary")
	public void activitiesEndBeforeStart() {
		int[] startTimes = { 5, 3, 1 };
		int[] endTimes = { 2, 2, 0 }; // Inverted times
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(selectedActivities).isEmpty(); // Depending on the sorting logic, the
													// result might vary.
	}

	@Test
	@Tag("valid")
	public void concurrentlyEndingActivitiesWithDifferentStarts() {
		int[] startTimes = { 1, 5, 3 };
		int[] endTimes = { 10, 10, 10 };
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		assertThat(selectedActivities).containsExactly(0); // Assumes selection of the
															// first activity to start
															// last if they end together.
	}

}