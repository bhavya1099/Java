
// ********RoostGPT********
/*
Test generated by RoostGPT for test python-abs-path using AI Type Open AI and AI Model gpt-4o

Test generated by RoostGPT for test python-abs-path using AI Type Open AI and AI Model gpt-4o

ROOST_METHOD_HASH=activitySelection_77631a6e2d
ROOST_METHOD_SIG_HASH=activitySelection_77631a6e2d

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/python-abs-path/1741672793/source/Java/src/test/java/com/thealgorithms/greedyalgorithms/ActivitySelectionTest.java
Tests:
    "@Test
@Test
public void testActivitySelection() {
    int[] start = { 1, 3, 0, 5, 8, 5 };
    int[] end = { 2, 4, 6, 7, 9, 9 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testSingleActivity() {
    int[] start = { 1 };
    int[] end = { 2 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));
    assertEquals(expected, result);
}
"
    "@Test
@Test
public void testNoOverlap() {
    int[] start = { 1, 2, 3 };
    int[] end = { 2, 3, 4 };
    ArrayList<Integer> result = ActivitySelection.activitySelection(start, end);
    ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2));
    assertEquals(expected, result);
}
"```
Scenario 1: Activities with Identical Start and End Times

Details:
  TestName: testIdenticalStartEndTimes
  Description: This test checks the scenario where all activities have identical start and end times, which might lead to selecting only one activity due to overlapping times.
Execution:
  Arrange: Set up start and end times arrays with identical values for all activities.
  Act: Invoke the activitySelection method with these arrays.
  Assert: Use JUnit assertions to check that only the first activity is selected.
Validation:
  The assertion verifies that the function correctly handles activities with overlapping times by selecting only the first activity. This is significant as it tests the function's ability to handle edge cases of identical activity times.

Scenario 2: Activities with Large Time Gaps

Details:
  TestName: testLargeTimeGaps
  Description: This test examines how the method handles activities that are spaced far apart in time, ensuring that all activities are selected when there are no overlaps.
Execution:
  Arrange: Set up start and end times with large gaps between the end of one activity and the start of the next.
  Act: Invoke the activitySelection method with these arrays.
  Assert: Use JUnit assertions to check that all activities are selected.
Validation:
  The assertion confirms that the function correctly identifies and selects all non-overlapping activities, demonstrating its effectiveness in handling non-overlapping scenarios.

Scenario 3: Reverse Ordered Activities

Details:
  TestName: testReverseOrderedActivities
  Description: This test checks how the function handles activities provided in reverse order of their end times, ensuring the sorting mechanism is effective.
Execution:
  Arrange: Set up start and end times arrays where activities are arranged in descending order of their end times.
  Act: Invoke the activitySelection method with these arrays.
  Assert: Use JUnit assertions to check that the correct set of non-overlapping activities are selected.
Validation:
  The assertion ensures that the function correctly sorts activities by end time and selects the appropriate ones, testing the sorting logic's robustness.

Scenario 4: Single Activity with Overlapping Times

Details:
  TestName: testSingleOverlappingActivity
  Description: This test checks a scenario where multiple activities overlap, but only one can be selected due to identical start and end times.
Execution:
  Arrange: Set up start and end times arrays with multiple activities having the same start and end times.
  Act: Invoke the activitySelection method with these arrays.
  Assert: Use JUnit assertions to check that only one activity is selected.
Validation:
  The assertion verifies the function's ability to handle overlapping activities correctly, ensuring only one is selected, which is crucial for maintaining the logic of non-overlapping selection.

Scenario 5: Activities with Zero Duration

Details:
  TestName: testZeroDurationActivities
  Description: This test verifies how the method handles activities with zero duration, where start and end times are the same.
Execution:
  Arrange: Set up start and end times arrays with activities having zero duration.
  Act: Invoke the activitySelection method with these arrays.
  Assert: Use JUnit assertions to check that all zero-duration activities are selected.
Validation:
  The assertion checks that the function correctly identifies and selects zero-duration activities, ensuring robustness in handling such edge cases.
```

These scenarios cover a wide range of edge cases and functional aspects of the `activitySelection` method, ensuring that it behaves as expected under various conditions.
*/

// ********RoostGPT********
package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.Test;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Comparator;
import static org.junit.jupiter.api.Assertions.assertEquals;

public class ActivitySelectionActivitySelectionTest {

	@Test
	public void testActivitySelection() {
		int[] startTimes = { 1, 3, 0, 5, 8, 5 };
		int[] endTimes = { 2, 4, 6, 7, 9, 9 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 3, 4));
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, result);
	}

	@Test
	public void testActivitySelectionWithNoActivities() {
		int[] startTimes = {};
		int[] endTimes = {};
		ArrayList<Integer> expected = new ArrayList<>();
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, result);
	}

	@Test
	public void testActivitySelectionWithSingleActivity() {
		int[] startTimes = { 1 };
		int[] endTimes = { 2 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0));
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, result);
	}

	@Test
	public void testActivitySelectionWithOverlappingActivities() {
		int[] startTimes = { 1, 2, 3, 4 };
		int[] endTimes = { 10, 3, 5, 6 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(1, 2, 3));
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, result);
	}

	@Test
	public void testActivitySelectionWithSameEndTimes() {
		int[] startTimes = { 1, 2, 3, 4 };
		int[] endTimes = { 5, 5, 5, 5 };
		ArrayList<Integer> expected = new ArrayList<>(Arrays.asList(0, 1, 2, 3));
		ArrayList<Integer> result = ActivitySelection.activitySelection(startTimes, endTimes);
		assertEquals(expected, result);
	}

}