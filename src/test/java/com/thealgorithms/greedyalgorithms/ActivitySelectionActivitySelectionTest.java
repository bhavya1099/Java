
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-report-test using AI Type Azure Open AI and AI Model roostgpt-4-32k

ROOST_METHOD_HASH=activitySelection_cce0da1981
ROOST_METHOD_SIG_HASH=activitySelection_79d2670611

"""
Scenario 1: Test with valid input where activities do not overlap

TestName: activitySelectionWithNonOverlappingActivities
Description: This test will check if the 'activitySelection' method correctly identifies and selects activities that do not overlap time. Since none of the activities overlap, all should be selected.
Execution:
  Arrange: Initialize startTimes and endTimes arrays with appropriate values which represent non-overlapping activities.
  Act: Invoke the 'activitySelection' method with the initialized arrays.
  Assert: Assert if the size of the returned list equals the size of the input arrays (as all activities should be selected).
Validation:
  The test verifies that the 'activitySelection' method correctly selects all non-overlapping activities. The significance of this test is to ensure that the algorithm does not unnecessarily reject activities that do not overlap with others.

Scenario 2: Test with valid input where some activities overlap

TestName: activitySelectionWithOverlappingActivities
Description: This test will check if the 'activitySelection' method correctly identifies and selects the maximum set of activities where no two activities overlap time.
Execution:
  Arrange: Initialize startTimes and endTimes arrays with some overlapping activities.
  Act: Invoke the 'activitySelection' method with the initialized arrays.
  Assert: Assert if the returned list contains only the non-overlapping activities.
Validation:
  The test validates that the 'activitySelection' method correctly omits overlapping activities and only selects non-overlapping activities. The importance of this test lies in verifying the functionality of the algorithm in correctly maximizing the number of selected activities.

Scenario 3: Test with empty arrays

TestName: activitySelectionWithEmptyArrays
Description: This test will check if the 'activitySelection' method can handle and return an appropriate result when empty arrays are provided as input.
Execution:
  Arrange: Initialize startTimes and endTimes arrays with no elements.
  Act: Invoke the 'activitySelection' method with the empty arrays.
  Assert: Assert if the returned ArrayList is also empty.
Validation:
  The test verifies the 'activitySelection' method can handle and appropriately return an empty ArrayList when no activities are provided as input. It checks how the code handles the edge case of an empty input array.

Scenario 4: Test with null input arrays

TestName: activitySelectionWithNullInput
Description: This test will check if the 'activitySelection' method can handle and return an appropriate result when null arrays are provided as input.
Execution:
  Arrange: Initialize startTimes and endTimes arrays as null.
  Act: Invoke the 'activitySelection' method with the null arrays.
  Assert: Check for appropriate error handling or return values.
Validation:
  The test checks the 'activitySelection' method's resilience to null input parameters. It is essential to verify how the algorithm handles null references, which represents a common source of exceptions in a real-world application.
"""
*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import java.util.ArrayList;
import java.util.Arrays;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import java.util.Comparator;

public class ActivitySelectionActivitySelectionTest {

	@Test
	@Tag("valid")
	public void activitySelectionWithNonOverlappingActivities() {
		// Arrange
		int[] startTimes = { 1, 3, 5, 7, 9 };
		int[] endTimes = { 2, 4, 6, 8, 10 };
		// Act
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertEquals(startTimes.length, selectedActivities.size(), "All non-overlapping activities should be selected");
	}

	@Test
	@Tag("valid")
	public void activitySelectionWithOverlappingActivities() {
		// Arrange
		int[] startTimes = { 1, 3, 0, 5, 8, 5, 6 };
		int[] endTimes = { 2, 4, 6, 7, 9, 9, 10 };
		// Act
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertEquals(Arrays.asList(0, 1, 3, 4), selectedActivities,
				"Only non-overlapping activities should be selected");
	}

	@Test
	@Tag("boundary")
	public void activitySelectionWithEmptyArrays() {
		// Arrange
		int[] startTimes = {};
		int[] endTimes = {};
		// Act
		ArrayList<Integer> selectedActivities = ActivitySelection.activitySelection(startTimes, endTimes);
		// Assert
		assertTrue(selectedActivities.isEmpty(), "Return list should be empty for empty input arrays");
	}

	@Test
	@Tag("invalid")
	public void activitySelectionWithNullInput() {
		// Arrange
		int[] startTimes = null;
		int[] endTimes = null;
		// Act & Assert
		assertThrows(NullPointerException.class, () -> ActivitySelection.activitySelection(startTimes, endTimes),
				"Should throw NullPointerException for null input arrays");
	}

}