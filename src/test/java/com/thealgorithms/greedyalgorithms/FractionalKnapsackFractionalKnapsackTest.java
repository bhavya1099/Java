
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-report-test using AI Type Claude AI and AI Model claude-3-7-sonnet-20250219

Test generated by RoostGPT for test java-report-test using AI Type Claude AI and AI Model claude-3-7-sonnet-20250219

ROOST_METHOD_HASH=fractionalKnapsack_0c67eb8939
ROOST_METHOD_SIG_HASH=fractionalKnapsack_feda426d78

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/java-report-test/1742880169/source/Java/src/test/java/com/thealgorithms/greedyalgorithms/FractionalKnapsackTest.java
Tests:
    "@Test
@Test
public void testFractionalKnapsackWithExampleCase() {
    int[] weight = { 10, 20, 30 };
    int[] value = { 60, 100, 120 };
    int capacity = 50;
    assertEquals(240, FractionalKnapsack.fractionalKnapsack(weight, value, capacity));
}
"
    "@Test
@Test
public void testFractionalKnapsackWithZeroCapacity() {
    int[] weight = { 10, 20, 30 };
    int[] value = { 60, 100, 120 };
    int capacity = 0;
    assertEquals(0, FractionalKnapsack.fractionalKnapsack(weight, value, capacity));
}
"
    "@Test
@Test
public void testFractionalKnapsackWithEmptyItems() {
    int[] weight = {};
    int[] value = {};
    int capacity = 50;
    assertEquals(0, FractionalKnapsack.fractionalKnapsack(weight, value, capacity));
}
"# Test Scenarios for FractionalKnapsack

```
Scenario 1: All Items Fit Within Capacity

Details:
  TestName: allItemsFitWithinCapacity
  Description: Tests the scenario where all items can fit within the knapsack capacity, so no fractional items are needed.
Execution:
  Arrange: Create weight array [5, 10, 15], value array [10, 30, 50], and capacity of 50.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 90.
Validation:
  This test verifies that when all items can fit in the knapsack, the method correctly calculates the total value by adding all item values. Since the total weight (30) is less than capacity (50), all items should be included completely.
```

```
Scenario 2: Fractional Item Required

Details:
  TestName: fractionalItemRequired
  Description: Tests the scenario where some items fit completely, but the last item needs to be included fractionally.
Execution:
  Arrange: Create weight array [10, 20, 30], value array [60, 100, 120], and capacity of 45.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 220.
Validation:
  This test verifies that the method correctly handles fractional items. After including the first two items (weight 30, value 160), there's capacity for half of the third item (15/30), which should add 60 more value, totaling 220.
```

```
Scenario 3: High Value Small Weight Items

Details:
  TestName: highValueSmallWeightItems
  Description: Tests the scenario where items with high value-to-weight ratios are prioritized correctly.
Execution:
  Arrange: Create weight array [5, 10, 15, 20], value array [100, 50, 30, 20], and capacity of 30.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 180.
Validation:
  This test verifies that the algorithm correctly prioritizes items with higher value-to-weight ratios. The first item has ratio 20, second has 5, third has 2, and fourth has 1. The optimal solution should take the first item (weight 5, value 100), then the second item (weight 10, value 50), and then the third item (weight 15, value 30), for a total value of 180.
```

```
Scenario 4: Large Capacity Exceeding Total Weight

Details:
  TestName: largeCapacityExceedingTotalWeight
  Description: Tests the scenario where the knapsack capacity exceeds the total weight of all items.
Execution:
  Arrange: Create weight array [10, 20, 30], value array [60, 100, 120], and capacity of 100.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 280.
Validation:
  This test verifies that when the capacity exceeds the total weight of all items, the method returns the sum of all item values. Since all items can fit completely, the total value should be 60 + 100 + 120 = 280.
```

```
Scenario 5: Single Item That Fits Completely

Details:
  TestName: singleItemFitsCompletely
  Description: Tests the scenario with only one item that fits completely within the capacity.
Execution:
  Arrange: Create weight array [10], value array [50], and capacity of 15.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 50.
Validation:
  This test verifies that the method works correctly with a single item that fits completely within the capacity. The expected value is 50 since the entire item can be included.
```

```
Scenario 6: Single Item That Fits Partially

Details:
  TestName: singleItemFitsPartially
  Description: Tests the scenario with only one item that fits partially within the capacity.
Execution:
  Arrange: Create weight array [20], value array [100], and capacity of 10.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 50.
Validation:
  This test verifies that the method correctly handles a single item that can only be included fractionally. Since only half of the item can fit (10/20), the expected value is 50 (half of 100).
```

```
Scenario 7: Items With Same Value-to-Weight Ratio

Details:
  TestName: itemsWithSameValueToWeightRatio
  Description: Tests the scenario where multiple items have the same value-to-weight ratio.
Execution:
  Arrange: Create weight array [10, 20, 30], value array [50, 100, 150], and capacity of 40.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 200.
Validation:
  This test verifies that the method correctly handles items with identical value-to-weight ratios (all are 5). In this case, any combination of items totaling 40 weight should give the same value. The expected result is 200, which could be achieved by taking the first two items completely (30 weight, 150 value) and 1/3 of the third item (10 weight, 50 value).
```

```
Scenario 8: Very Small Capacity

Details:
  TestName: verySmallCapacity
  Description: Tests the scenario with a very small capacity that can only fit a fraction of the smallest item.
Execution:
  Arrange: Create weight array [10, 20, 30], value array [60, 100, 120], and capacity of 5.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 30.
Validation:
  This test verifies that the method correctly handles a very small capacity. The item with the highest value-to-weight ratio is the first one (ratio 6), so half of it should be included, resulting in a value of 30.
```

```
Scenario 9: Negative Values

Details:
  TestName: negativeValues
  Description: Tests the scenario where some items have negative values.
Execution:
  Arrange: Create weight array [10, 20, 30], value array [-60, 100, 120], and capacity of 50.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 220.
Validation:
  This test verifies that the method correctly handles negative values by not including items with negative value-to-weight ratios. The optimal solution should only include the second and third items, for a total value of 220.
```

```
Scenario 10: Zero Weights

Details:
  TestName: zeroWeights
  Description: Tests the scenario where some items have zero weight.
Execution:
  Arrange: Create weight array [0, 20, 30], value array [60, 100, 120], and capacity of 40.
  Act: Call fractionalKnapsack(weight, value, capacity).
  Assert: Assert that the returned value equals 160.
Validation:
  This test verifies that the method correctly handles items with zero weight. An item with zero weight has an infinite value-to-weight ratio and should always be included. The expected result is 60 (from the zero-weight item) + 100 (from the second item), totaling 160.
```
*/

// ********RoostGPT********

package com.thealgorithms.greedyalgorithms;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.*;
import java.util.Arrays;
import java.util.Comparator;

class FractionalKnapsackFractionalKnapsackTest {

	@Test
	@Tag("valid")
	@DisplayName("All items fit within capacity")
	void allItemsFitWithinCapacity() {
		int[] weight = { 5, 10, 15 };
		int[] value = { 10, 30, 50 };
		int capacity = 50;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(90, result);
	}

	@Test
	@Tag("valid")
	@DisplayName("Fractional item required")
	void fractionalItemRequired() {
		int[] weight = { 10, 20, 30 };
		int[] value = { 60, 100, 120 };
		int capacity = 45;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(220, result);
	}

	@Test
	@Tag("valid")
	@DisplayName("High value small weight items")
	void highValueSmallWeightItems() {
		int[] weight = { 5, 10, 15, 20 };
		int[] value = { 100, 50, 30, 20 };
		int capacity = 30;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(180, result);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Large capacity exceeding total weight")
	void largeCapacityExceedingTotalWeight() {
		int[] weight = { 10, 20, 30 };
		int[] value = { 60, 100, 120 };
		int capacity = 100;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(280, result);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Single item that fits completely")
	void singleItemFitsCompletely() {
		int[] weight = { 10 };
		int[] value = { 50 };
		int capacity = 15;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(50, result);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Single item that fits partially")
	void singleItemFitsPartially() {
		int[] weight = { 20 };
		int[] value = { 100 };
		int capacity = 10;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(50, result);
	}

	@Test
	@Tag("valid")
	@DisplayName("Items with same value-to-weight ratio")
	void itemsWithSameValueToWeightRatio() {
		int[] weight = { 10, 20, 30 };
		int[] value = { 50, 100, 150 };
		int capacity = 40;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(200, result);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Very small capacity")
	void verySmallCapacity() {
		int[] weight = { 10, 20, 30 };
		int[] value = { 60, 100, 120 };
		int capacity = 5;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(30, result);
	}

	@Test
	@Tag("invalid")
	@DisplayName("Negative values")
	void negativeValues() {
		int[] weight = { 10, 20, 30 };
		int[] value = { -60, 100, 120 };
		int capacity = 50;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(220, result);
	}

	@Test
	@Tag("boundary")
	@DisplayName("Zero weights")
	void zeroWeights() {
		int[] weight = { 0, 20, 30 };
		int[] value = { 60, 100, 120 };
		int capacity = 40;

		int result = FractionalKnapsack.fractionalKnapsack(weight, value, capacity);

		assertEquals(160, result);
	}

}